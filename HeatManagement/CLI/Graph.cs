using System;
using System.Collections.Generic;
using AnsiRenderer;

namespace HeatManagement;

static partial class CLI
{
    public static void GraphDrawer(string name, Color color, List<Tuple<DateTime, DateTime>> times, Dictionary<Tuple<DateTime, DateTime>, double> values)
    {
        //TODO actual CLI of the graphs
    }
}